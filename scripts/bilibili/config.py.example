"""B 站 API 凭证配置示例 - 复制为 config.py 并填入凭证"""

from bilibili_api import Credential

SESSDATA = ""  # 必填：从浏览器 Cookie 获取
BILI_JCT = ""  # 可选
BUVID3 = ""    # 可选


def get_credential() -> Credential:
    """获取登录凭证"""
    if not SESSDATA:
        raise ValueError("请先配置 SESSDATA")
    return Credential(sessdata=SESSDATA, bili_jct=BILI_JCT, buvid3=BUVID3)


# 获取 SESSDATA: 登录 B 站 → F12 → Application → Cookies → SESSDATA
